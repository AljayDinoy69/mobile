import React from 'react';
import { Text, View } from 'react-native';

const ResponderMap = () => (
  <View style={{ width: '100%', height: 300, borderRadius: 10, marginBottom: 10, alignItems: 'center', justifyContent: 'center', backgroundColor: '#eee' }}>
    <Text style={{ color: '#888' }}>Map is only available on mobile/native.</Text>
  </View>
);

export default ResponderMap; 